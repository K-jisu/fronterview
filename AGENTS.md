# Agent Working Rules

## Purpose
- 이 문서는 프로젝트에서 작업하는 에이전트들의 공통 작업 원칙을 정의한다.
- 모든 에이전트는 코드 작성이나 구조 변경 전에 이 문서를 먼저 확인한다.

## Project Understanding
- 코드 작성 전에 반드시 [PLAN.md](D:/VSCode/fronterview/PLAN.md)를 꼼꼼하게 읽고 현재 제품 기획과 범위를 이해한다.
- 기획서만으로 부족하면 관련 코드와 폴더 구조를 먼저 읽고 현재 상태를 파악한 뒤 작업한다.
- 프로젝트를 충분히 이해하기 전에는 성급하게 구현 방향을 정하지 않는다.

## Permission Before Coding
- 사용자가 명시적으로 구현을 요청하지 않았다면 코드를 작성하지 않는다.
- 사용자가 `내가 알아서 해봐`, `구현해`, `진행해`처럼 분명하게 승인하기 전에는 기획, 분석, 설계, 문서화까지만 수행한다.
- 코드 수정, 파일 생성, 구조 변경, 의존성 변경이 필요한 경우 먼저 사용자에게 허락을 받는다.

## Architecture And Structure
- 프로젝트 구조는 기존과 동일하게 `FSD(Feature-Sliced Design)` 원칙에 맞춰 유지한다.
- `app`, `widgets`, `features`, `entities`, `shared`의 책임을 섞지 않는다.
- 한 파일에 너무 많은 책임을 몰아넣지 않는다.
- 구현 시에는 가능한 한 모듈화하고, 역할이 다른 로직은 분리한다.

## Clarification Before Abstraction
- 사용자 지시가 명확하지 않다면 임의로 추상화하지 않는다.
- 요구사항이 불명확하거나 여러 해석이 가능하면 먼저 사용자에게 확인한다.
- 확실하지 않은 상태에서 범용 구조, 과한 추상화, 선제적 확장을 만들지 않는다.

## Dependency Rules
- 라이브러리를 임의로 설치하지 않는다.
- 새 라이브러리가 필요하다고 판단되면 먼저 다음 내용을 설명하고 허락을 받는다.
  - 왜 이 라이브러리가 필요한지
  - 설치해서 얻는 이점이 무엇인지
  - 기존 의존성이나 직접 구현으로 대체 가능한지
- 사용자 승인 없이 패키지 매니저 명령을 실행하지 않는다.

## Code Style
- 주석은 필요한 곳에 남긴다.
- 다만 불필요한 설명형 주석보다는, 의도나 이유를 이해하는 데 필요한 주석을 작성한다.
- 파일과 모듈 이름은 역할이 드러나도록 작성한다.
- 구조보다 구현이 앞서지 않게 하고, 현재 기획 범위를 넘는 코드는 추가하지 않는다.

## Communication Rules
- 작업 전에는 무엇을 이해했고 무엇을 하려는지 짧게 공유한다.
- 분석 단계와 구현 단계를 명확히 구분해서 소통한다.
- 사용자 승인 없이 구현을 시작하지 않는다.
- 진행 중 불확실성이 생기면 추측으로 밀어붙이지 말고 사용자에게 질문한다.
