# Frontend Interview Archive Product Plan

## Change Log

### v0.1.0 - 2026-04-15
- 초기 제품 기획서 작성
- 제품 포지셔닝을 `주니어 프론트엔드 취준생 대상 큐레이션형 면접 질문 아카이브`로 정의
- 1차 범위를 `질문 아카이브 완성형`으로 확정하고 `플래시카드`, `모의면접`, `AI 코치`를 후속 범위로 분리
- 질문 목록, 질문 상세, 마이페이지, 계정 정책, 성공 지표, 테스트 기준의 기본 방향 확정

## Summary
- 제품은 `주니어 프론트엔드 취준생`을 위한 `큐레이션형 면접 질문 아카이브`로 정의한다.
- 핵심 가치는 방대한 기능이 아니라 `좋은 질문을 빠르게 찾고, 면접 답변 관점에서 이해하고, 다시 복습하러 돌아오게 만드는 경험`이다.
- 첫 기획 범위는 `질문 아카이브 완성형`으로 잡고, `플래시카드`, `모의면접`, `AI 코치`는 후속 단계로 명시적으로 제외한다.
- 계정은 필수가 아니라 `옵션형`으로 두고, 질문 탐색은 비로그인으로 열어두며 저장/개인화 기능만 계정과 연결한다.

## Product Definition
- 기본 정보구조는 `주제 중심`으로 설계한다: HTML/CSS, JavaScript, React, 브라우저, 네트워크, 성능, 접근성, 상태관리, 테스트.
- 홈은 서비스 소개보다 `질문 탐색 진입`을 우선한다. 주요 주제, 추천 질문, “질문 보러가기” CTA를 중심으로 재정렬한다.
- 질문 목록 페이지는 제품의 핵심 허브로 둔다. 필수 요소는 `검색`, `주제 필터`, `난이도 필터`, `큐레이션 순 정렬`, `질문 카드 목록`이다.
- 질문 상세 페이지는 `답변 가이드형`으로 고정한다. 한 질문은 다음 섹션을 가진다: 질문 원문, 왜 묻는지, 핵심 답변 포인트, 좋은 답변 예시 방향, 꼬리질문, 자주 하는 실수, 관련 질문.
- 마이페이지는 `저장한 질문`과 `최근 본 질문`만 우선 제공한다. 목표/통계/추천 로직은 넣지 않는다.
- 로그인/회원가입은 메인 가치가 아니라 보조 수단으로 위치시킨다. 비로그인 사용자는 탐색과 열람 가능, 로그인 사용자는 저장 목록과 열람 기록 동기화 가능으로 정의한다.
- 네비게이션은 `Home / Questions / My Page` 중심으로 단순화한다. `Flashcards`는 1차 기획 범위에서 제외하고 노출하지 않거나 `Coming Soon`으로만 다룬다.

## Public Interfaces And Data Shape
- 질문 콘텐츠 단위 `Question`은 최소한 다음 필드를 가진다: `id`, `slug`, `title`, `topic`, `subtopic`, `difficulty`, `summary`, `answerPoints[]`, `followUps[]`, `commonMistakes[]`, `relatedQuestionIds[]`.
- 사용자별 상태 `UserQuestionState`는 최소한 다음 필드를 가진다: `questionId`, `bookmarked`, `lastViewedAt`.
- 질문 목록 화면은 `keyword`, `topic`, `difficulty` 기준 필터를 지원한다.
- 상세 페이지 URL은 `questions/[slug]` 구조를 기본으로 한다.
- 마이페이지는 `bookmarks`와 `recentViews` 두 컬렉션을 읽는 구조로 고정한다.
- 계정 미사용 상태의 저장/최근본은 기본적으로 로컬 저장소 기반으로 정의하고, 로그인 시 서버 동기화 가능한 구조로 설계한다.

## Experience And Success Criteria
- 대표 사용자 흐름은 `홈 진입 → 주제 선택 → 질문 목록 탐색 → 질문 상세 열람 → 북마크 또는 이탈 → 재방문 후 최근 본 질문 재열람`이다.
- 제품 성공 기준은 `재방문/복습률`로 둔다. 단순 조회 수보다 “다시 돌아와서 이어보는가”를 우선 지표로 삼는다.
- 필수 이벤트는 `question_list_view`, `question_open`, `filter_apply`, `bookmark_add`, `bookmark_remove`, `recent_revisit`, `login_for_sync` 정도로 정의한다.
- 홈 카피와 UI는 “학습 플랫폼”보다 “면접 대비용 정리된 질문 아카이브” 톤으로 통일한다.

## Test Plan
- 비로그인 사용자가 홈에서 질문 목록으로 진입하고, 주제/난이도 필터와 검색으로 원하는 질문을 찾을 수 있어야 한다.
- 질문 상세에서 답변 가이드, 꼬리질문, 실수 포인트, 관련 질문이 일관된 포맷으로 보여야 한다.
- 비로그인 상태에서도 최근 본 질문이 기록되고 다음 방문에서 다시 보여야 한다.
- 로그인 사용자는 저장한 질문이 마이페이지에서 보여야 하며, 저장/해제 상태가 안정적으로 반영되어야 한다.
- 질문이 없는 주제, 검색 결과 없음, 비로그인 마이페이지 접근, 로그인 전 저장 시도 같은 빈 상태와 전환 흐름이 정의되어야 한다.
- `Flashcards`, `Mock Interview`, `AI 추천`은 1차 범위 밖으로 유지되어 핵심 흐름을 방해하지 않아야 한다.

## Assumptions And Defaults
- 초기 콘텐츠는 `운영자 큐레이션`만 사용하며 사용자 생성 질문은 다루지 않는다.
- 첫 출시에서는 `질문 아카이브 품질`이 최우선이며, 게임화/통계/추천 기능은 뒤로 미룬다.
- 계정은 선택 사항이므로 제품의 핵심 가치가 인증에 의존하면 안 된다.
- 현재 코드베이스의 `login`, `signup`, `mypage`, `flashcards` 라우트는 유지 가능하되, 기획상 우선순위는 `questions` 중심으로 재배치한다.
